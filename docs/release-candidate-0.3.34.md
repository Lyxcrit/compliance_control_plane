# Release Candidate 0.3.34

- Bound the Requirements Explorer drilldown specifically to the visible `Open check` column.
- Pass framework and requirement values using Simple XML form tokens.
- Keep the handoff in the current browser tab so the selected check is immediately verifiable.
- Added regression assertions for the conditional drilldown and form-token URL.
