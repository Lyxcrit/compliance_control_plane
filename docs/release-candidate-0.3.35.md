# Release Candidate 0.3.35

- Made the visible `Open check` cell carry the requirement key as its value.
- The drilldown now passes the clicked cell value through `$click.value$`, avoiding row-field loss during cell navigation.
- The selected framework remains sourced from the Requirements framework token.
- Added regression assertions for the clicked requirement handoff.
